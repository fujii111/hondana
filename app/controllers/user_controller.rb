class UserController < ApplicationController
  def index
    # @members_id = Member.find(params[:id])
    # @members = Member.find_by_sql(["SELECT * FROM members WHERE members.id = :id ",{:id => @members_id}])
  end
end
