<%= form_for(@bookgenre) do |f| %>
  <% if @bookgenre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookgenre.errors.count, "error") %> prohibited this bookgenre from being saved:</h2>

      <ul>
      <% @bookgenre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sort %><br>
    <%= f.number_field :sort %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
